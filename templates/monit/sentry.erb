check process sentry with
   pidfile <%= sentry_pidfile %>
   start program = "/etc/init.d/sentry_init start"
   stop program = "/etc/init.d/sentry_init stop"
   if failed host 127.0.0.1 port 9000 then restart
   if 5 restarts within 5 cycles then timeout